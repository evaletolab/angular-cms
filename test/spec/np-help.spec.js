describe('np-help', function () {


    beforeEach(module('npHelp'));

    beforeEach(inject(function ($rootScope, $compile, $q) {


        scope = $rootScope.$new(true);

    }));

    it('your first test here', inject(function ($compile, $rootScope) {
    }));

  
});
